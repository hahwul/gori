# Short block parameter names (rs, p, c, i) are idiomatic Crystal; the few
# inherently-branchy parsers/dispatchers sit just over the default complexity
# bar. Everything else is linted normally.
Naming/BlockParameterName:
  Enabled: false

Metrics/CyclomaticComplexity:
  MaxComplexity: 12
