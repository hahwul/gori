# STUB — replaced by the real section in feat/1090-agent-channel; do not merge this version
module Gori::Settings
  DEFAULT_MCP_CHANNELS = false
  class_property? mcp_channels : Bool = DEFAULT_MCP_CHANNELS
end
